# Phase10.1 Results

Phase10.1 wrapper-level simulation outputs are written here.

Expected files after running the AXI-Stream wrapper TB:

- `tb_sr_core_axis_wrapper_result.txt`

The generated output image text is written to:

- `model_lite/sr_core/pic/test_pic/result/sr_output_uint8_axis_wrapper.txt`
